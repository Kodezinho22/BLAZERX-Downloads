name: Publish ESTUDEX v1.9.3 Hotfix 22 Cloud Social

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hotfix 22 publish is temporarily paused while payload transport is repaired."
